{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".amplify-hosting/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
