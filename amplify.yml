version: 1
applications:
  - appRoot: demo
    frontend:
      phases:
        preBuild:
          commands:
            - nvm install 18
            - npm i -g pnpm
            - pnpm config set store-dir .pnpm-store
            - pnpm i
        build:
          commands:
            - pnpm run build
      artifacts:
        baseDirectory: .amplify-hosting
        files:
          - '**/*'
      cache:
        paths:
          - .pnpm-store/**/*